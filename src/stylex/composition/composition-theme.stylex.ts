import * as stylex from '@stylexjs/stylex'

/** Semantic surfaces that an application shell may scope without widening primitives. */
export const compositionTheme = stylex.defineVars({
  canvas: 'var(--muted)',
})

