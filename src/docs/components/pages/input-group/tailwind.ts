import { textPreviewProgram } from '@/docs/components/pages/authored-page'; import { inputGroupFixtures } from '@/docs/components/pages/input-group/shared'; import * as InputGroup from '@/ui/input-group';
export const inputGroupTailwindPreviewProgram = textPreviewProgram('input-group', ['', ''], (index, value, onInput, h) => { const fixture = inputGroupFixtures[index] ?? inputGroupFixtures[0]; const addon = InputGroup.inputGroupAddon({ ...(fixture.trailing ? { align: 'inline-end' as const } : {}), children: [InputGroup.inputGroupText({ children: [fixture.addon] }, h)] }, h); const input = InputGroup.inputGroupInput({ id: `docs-input-group-${String(index)}`, value, onInput, placeholder: fixture.placeholder }, h); return InputGroup.inputGroup({ class: 'max-w-sm', children: fixture.trailing ? [input, addon] : [addon, input] }, h); });
