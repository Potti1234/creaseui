import { authoredPage } from '@/docs/components/pages/authored-page'; import { sidebarExamples } from '@/docs/components/pages/sidebar/shared'; import { sidebarTailwindPreviewProgram } from '@/docs/components/pages/sidebar/tailwind';
export const sidebarPage = authoredPage({ slug: 'sidebar', title: 'Sidebar', kind: 'submodel', previewProgram: sidebarTailwindPreviewProgram, definition: { kind: 'submodel', description: 'Builds a responsive application shell with persistent desktop collapse state and separate mobile disclosure state.', architecture: 'Sidebar’s exported Model owns desktop open, mobile open, and persistence key. The parent delegates Message, maps the cookie-persistence Command, and registers the cmd/ctrl+B subscription. View helpers receive derived expanded/collapsed state explicitly—there is no React context.', apiHref: 'https://foldkit.dev/guide/subscriptions', composition: 'SidebarProvider (derived state)\n├── Sidebar\n│   ├── header\n│   ├── content → groups → menu items\n│   ├── footer\n│   └── collapse rail\n└── SidebarInset\n    └── trigger + application content', styling: 'Desktop and mobile states are deliberately separate. Choose icon, offcanvas, or none per shell; use the contained presentation only for bounded previews.', accessibility: 'Triggers have screen-reader labels, mobile presentation is a named aside with a dismiss backdrop, and navigation items remain ordinary buttons or links. Persisted visual state must not hide the only route to content.', keyboard: [['Ctrl/⌘ + B', 'Toggles desktop sidebar state through a document subscription.'], ['Tab', 'Moves through the trigger and visible navigation controls.'], ['Enter / Space', 'Activates the focused navigation control or trigger.']], examples: sidebarExamples('tailwind'), stylexExamples: sidebarExamples('stylex') } });
