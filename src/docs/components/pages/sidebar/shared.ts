import type { DocsExample } from '@/docs/components/page-definition'; import { foldkitApplication } from '@/docs/components/pages/authored-page';
export const sidebarFixtures = [{ title: 'Persistent shell', description: 'The complete application includes the child update, persistence Command mapping, keyboard subscription, mobile state, and composed view.' }, { title: 'Static structure', description: 'Use collapsible none when a bounded navigation column should always remain present.' }] as const;
const source = (renderer: 'tailwind' | 'stylex') => { const sx = renderer === 'stylex'; return foldkitApplication({ title: 'Sidebar — Persistent application shell', imports: `import { Schema as S } from 'effect'\nimport { Command, Runtime, Subscription } from 'foldkit'\nimport { type Document, type HtmlBuilder } from 'foldkit/html'\nimport { m } from 'foldkit/message'\n${sx ? "import * as stylex from '@stylexjs/stylex'\n\nconst styles = stylex.create({ brand: { fontWeight: 600, paddingBlock: '0.25rem', paddingInline: '0.5rem' }, header: { alignItems: 'center', borderBottomColor: 'var(--border)', borderBottomStyle: 'solid', borderBottomWidth: 1, display: 'flex', height: '3rem', paddingInline: '0.75rem' }, main: { padding: '1.5rem' } })\n" : ''}\nimport * as Sidebar from '@/${sx ? 'stylex' : 'ui'}/sidebar'`, model: `export const Model = S.Struct({ sidebar: Sidebar.Model })\nexport type Model = typeof Model.Type`, messages: `export const GotSidebarMessage = m('GotSidebarMessage', { message: Sidebar.Message })\nexport const Message = S.Union([GotSidebarMessage])\nexport type Message = typeof Message.Type`, init: `export const init = (): readonly [Model, ReadonlyArray<Command.Command<Message>>] => [{ sidebar: Sidebar.init({ defaultOpen: true, storageKey: 'workspace_sidebar' }) }, []]`, update: `export const update = (model: Model, message: Message): readonly [Model, ReadonlyArray<Command.Command<Message>>] => { switch (message._tag) { case 'GotSidebarMessage': { const [sidebar, commands] = Sidebar.update(model.sidebar, message.message); return [{ ...model, sidebar }, Command.mapMessages(commands, next => GotSidebarMessage({ message: next }))] } } }`, subscriptions: `export const subscriptions = Subscription.make<Model, Message>()(() => ({ sidebarShortcut: Subscription.persistent(Sidebar.shortcut(next => GotSidebarMessage({ message: next }))) }))`, view: `const navigation = (h: HtmlBuilder<Message>) => Sidebar.sidebarMenu({ children: ['Playground', 'Models', 'Documentation'].map((label, index) => Sidebar.sidebarMenuItem({ children: [Sidebar.sidebarMenuButton({ isActive: index === 0, children: [label] }, h)] }, h)) }, h)\nexport const view = (model: Model, h: HtmlBuilder<Message>): Document => { const state: Sidebar.SidebarState = model.sidebar.isOpen ? 'expanded' : 'collapsed'; const toggle = GotSidebarMessage({ message: Sidebar.Toggled() }); return { title: 'Sidebar — Persistent application shell', body: Sidebar.sidebarProvider({ state, children: [Sidebar.sidebar({ state, collapsible: 'icon', isMobileOpen: model.sidebar.isMobileOpen, onMobileDismiss: GotSidebarMessage({ message: Sidebar.SetMobileOpen({ isOpen: false }) }), children: [Sidebar.sidebarHeader({ children: [h.div([${sx ? "h.Class(stylex.props(styles.brand).className ?? '')" : "h.Class('px-2 py-1 font-semibold')"}], ['Acme Inc.'])] }, h), Sidebar.sidebarContent({ children: [Sidebar.sidebarGroup({ children: [Sidebar.sidebarGroupLabel({ children: ['Platform'] }, h), Sidebar.sidebarGroupContent({ children: [navigation(h)] }, h)] }, h)] }, h), Sidebar.sidebarFooter({ children: ['Workspace'] }, h), Sidebar.sidebarRail({ onClick: toggle }, h)] }, h), Sidebar.sidebarInset({ children: [h.header([${sx ? "h.Class(stylex.props(styles.header).className ?? '')" : "h.Class('flex h-12 items-center border-b px-3')"}], [Sidebar.sidebarTrigger({ onClick: toggle }, h)]), h.div([${sx ? "h.Class(stylex.props(styles.main).className ?? '')" : "h.Class('p-6')"}], ['Application content'])] }, h)] }, h) } }` }); };
export const sidebarExamples = (renderer: 'tailwind' | 'stylex'): ReadonlyArray<DocsExample> => sidebarFixtures.map(fixture => ({ ...fixture, code: source(renderer) }));
