import type { HtmlBuilder } from 'foldkit/html'; import type { StyleXExamplePreviewProvider } from '@/docs/components/page-definition'; import * as Button from '@/stylex/button'; import * as Toast from '@/stylex/toast';
export const toastStyleXPreview: StyleXExamplePreviewProvider = <Msg>(_index: number, model: unknown, send: (json: string) => Msg, h: HtmlBuilder<Msg>) => { const notifications = (model as { notifications: Toast.Model }).notifications; const message = (value: object) => send(JSON.stringify(value)); return h.div([], [Button.button({ onClick: message({ _tag: 'ShowedToastPreview' }), children: ['Show toast'] }, h), Toast.toast({ model: notifications, toParentMessage: next => message({ _tag: 'GotToastPreviewMessage', message: next }), ariaLabel: 'Toast notifications' }, h)]); };
