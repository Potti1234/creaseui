import { authoredPage } from '@/docs/components/pages/authored-page'; import { chartExamples } from '@/docs/components/pages/chart/shared'; import { chartTailwindPreviewProgram } from '@/docs/components/pages/chart/tailwind';
export const chartPage = authoredPage({ slug: 'chart', title: 'Chart', kind: 'recipe', previewProgram: chartTailwindPreviewProgram, definition: { kind: 'recipe', description: 'Pure SVG chart recipes plus a Foldkit lifecycle adapter for Apache ECharts.', architecture: 'Keep serializable chart data and configuration in the parent. Pure SVG helpers render directly; ECharts is acquired by Mount, updated through SyncChart, observed for resize, and disposed when the host leaves the DOM.', apiHref: 'https://foldkit.dev/guide/html', composition: 'Parent domain Model\n├── serializable data + variant\n├── ECharts Mount resource\n│   ├── ResizeObserver\n│   ├── SyncChart Command\n│   └── disposal finalizer\n└── accessible summary or data table', styling: 'Chart colors come from theme tokens. Size the host; the lifecycle adapter resolves tokens when mounted and ECharts handles its canvas.', accessibility: 'Every renderer exposes an image role and accessible name. The ECharts adapter requires a summary or data table because canvas tooltips are supplementary and are not the keyboard-accessible data source.', examples: chartExamples('tailwind'), stylexExamples: chartExamples('stylex') } });
