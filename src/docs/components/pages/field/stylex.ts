import * as stylex from '@stylexjs/stylex'; import type { HtmlBuilder } from 'foldkit/html'; import type { StyleXExamplePreviewProvider } from '@/docs/components/page-definition'; import * as Field from '@/stylex/field'; import * as Input from '@/stylex/input';
const styles = stylex.create({ field: { maxWidth: '24rem' } });
export const fieldStyleXPreview: StyleXExamplePreviewProvider = <Msg>(index: number, model: unknown, onMessageJson: (json: string) => Msg, h: HtmlBuilder<Msg>) => { const value = (model as { value: string }).value; const onInput = (next: string) => onMessageJson(JSON.stringify({ _tag: 'ChangedPreviewText', value: next })); const control = (id: string, label: string, description?: string, error?: string) => Field.controlField({ id, label, ...(description === undefined ? {} : { description }), ...(error === undefined ? {} : { error }), layoutStyle: styles.field, toControl: (parts, controlH) => Input.input({ id: parts.controlId, value, onInput, placeholder: 'Ada Lovelace', ...(parts.describedBy === undefined ? {} : { describedBy: parts.describedBy }), isInvalid: parts.isInvalid, isDisabled: parts.isDisabled }, controlH) }, h); if (index === 0) return control('docs-field-name', 'Display name', 'Shown on your public profile.'); if (index === 1) return control('docs-field-error', 'Display name', undefined, 'Display name is required.'); if (index === 2) return control('docs-field-username', 'Username', 'Availability is checked after each edit.', value.length < 3 ? 'Use at least three characters.' : undefined); return Field.fieldGroup({ layoutStyle: styles.field, children: [control('docs-field-first', 'First name'), control('docs-field-last', 'Last name')] }, h); };
