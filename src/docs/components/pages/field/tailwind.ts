import { textPreviewProgram } from '@/docs/components/pages/authored-page'; import * as Field from '@/ui/field'; import * as Input from '@/ui/input';
export const fieldTailwindPreviewProgram = textPreviewProgram('field', ['', '', 'ad', ''], (index, value, onInput, h) => { const control = (id: string, label: string, description?: string, error?: string) => Field.controlField({ id, label, ...(description === undefined ? {} : { description }), ...(error === undefined ? {} : { error }), class: 'max-w-sm', toControl: (parts, controlH) => Input.input({ id: parts.controlId, value, onInput, placeholder: 'Ada Lovelace', ...(parts.describedBy === undefined ? {} : { describedBy: parts.describedBy }), isInvalid: parts.isInvalid, isDisabled: parts.isDisabled }, controlH) }, h); if (index === 0) return control('docs-field-name', 'Display name', 'Shown on your public profile.'); if (index === 1) return control('docs-field-error', 'Display name', undefined, 'Display name is required.'); if (index === 2) return control('docs-field-username', 'Username', 'Availability is checked after each edit.', value.length < 3 ? 'Use at least three characters.' : undefined); return Field.fieldGroup({ class: 'max-w-sm', children: [control('docs-field-first', 'First name'), control('docs-field-last', 'Last name')] }, h); });
