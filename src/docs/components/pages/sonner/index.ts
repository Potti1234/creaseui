import { authoredPage } from '@/docs/components/pages/authored-page'; import { notificationDefinition } from '@/docs/components/pages/notification-page'; import { sonnerConfig, sonnerExamples } from '@/docs/components/pages/sonner/shared'; import { sonnerTailwindPreviewProgram } from '@/docs/components/pages/sonner/tailwind';
const base = notificationDefinition(sonnerConfig);
export const sonnerPage = authoredPage({ slug: 'sonner', title: 'Sonner', kind: 'submodel', previewProgram: sonnerTailwindPreviewProgram, definition: { ...base, architecture: `${base.architecture} Sonner and Toast export the same Model, Message, update, timer, and OutMessage identities. The Sonner name changes only the compatibility surface and viewport skin.`, accessibility: `${base.accessibility} The compatibility layer does not alter live-region priority or keyboard behavior.`, examples: sonnerExamples('tailwind'), stylexExamples: sonnerExamples('stylex') } });
