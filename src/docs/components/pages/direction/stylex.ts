import * as stylex from '@stylexjs/stylex'; import type { HtmlBuilder } from 'foldkit/html'; import type { StyleXExamplePreviewProvider } from '@/docs/components/page-definition'; import * as Button from '@/stylex/button'; import * as Direction from '@/stylex/direction';
import { tokens } from '../../../../stylex/tokens.stylex';
const styles = stylex.create({ row: { gap: '0.75rem', alignItems: 'center', display: 'flex', }, token: { display: 'inline-block', fontFamily: 'monospace' }, count: { color: tokens.mutedForeground, fontSize: '0.875rem' } });
export const directionStyleXPreview: StyleXExamplePreviewProvider = <Msg>(index: number, model: unknown, onMessageJson: (json: string) => Msg, h: HtmlBuilder<Msg>) => index === 0 ? h.div([h.Class(stylex.props(styles.row).className ?? '')], [Direction.direction({ direction: 'rtl', children: [Button.button({ onClick: onMessageJson(JSON.stringify({ _tag: 'ClickedPreview' })), children: ['التالي', '←'] }, h), h.span([h.Class(stylex.props(styles.count).className ?? '')], [`Clicked ${(model as { interactionCount: number }).interactionCount} times`])] }, h)]) : Direction.direction({ direction: 'rtl', children: ['الإصدار ', h.span([h.Class(stylex.props(styles.token).className ?? '')], [Direction.direction({ direction: 'ltr', children: ['v0.163.0'] }, h)])] }, h);
