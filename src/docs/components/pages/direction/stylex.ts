import * as stylex from '@stylexjs/stylex'; import type { HtmlBuilder } from 'foldkit/html'; import type { StyleXExamplePreviewProvider } from '@/docs/components/page-definition'; import * as Button from '@/stylex/button'; import * as Direction from '@/stylex/direction';
const styles = stylex.create({ row: { alignItems: 'center', display: 'flex', gap: '0.75rem' }, token: { display: 'inline-block', fontFamily: 'monospace' } });
export const directionStyleXPreview: StyleXExamplePreviewProvider = <Msg>(index: number, _model: unknown, onMessageJson: (json: string) => Msg, h: HtmlBuilder<Msg>) => index === 0 ? h.div([h.Class(stylex.props(styles.row).className ?? '')], [Direction.direction({ direction: 'rtl', children: [Button.button({ onClick: onMessageJson(JSON.stringify({ _tag: 'ClickedPreview' })), children: ['التالي', '←'] }, h)] }, h)]) : Direction.direction({ direction: 'rtl', children: ['الإصدار ', h.span([h.Class(stylex.props(styles.token).className ?? '')], [Direction.direction({ direction: 'ltr', children: ['v0.148.2'] }, h)])] }, h);
